# 블로그 홈판 자동화 (코덱스판) — 사용법

에디 강사가 매일 돌리는 홈판 글쓰기 자동화를 **수강생용**으로 정리한 것입니다.
글을 만들고 → **코덱스(유료 챗지피티)가 점수를 매겨 고칠 곳을 짚고** → 클로드 코드가 다시 써서
→ 무료 사진을 넣어 → **네이버에 임시저장**까지 한 번에 합니다. 고치는 건 클로드 코드가 해줍니다.

> 코덱스(유료 챗지피티)가 없어도 됩니다. 그러면 초안 그대로 사진 넣어 임시저장까지 진행됩니다.
> 피드백까지 자동으로 받고 싶으면 **무료 패키지 ①(blog-auto-starter)**를 쓰세요.

---

## 처음 1회만 (설치)

1. 받은 압축파일을 풀고, 나온 폴더를 **내 문서 → 「블로그 홈판 수익화」** 로 옮기기
   (폴더 이름을 `블로그 홈판 수익화` 로 바꿔 두면 다음에 찾기 쉽습니다)
2. **설치 파일 더블클릭**: 윈도우 `설치_윈도우.bat` / 맥 `설치_맥.command`
   → "설치 완료"가 나올 때까지 기다리기 (5~10분)
3. 메모장으로 **내정보.txt** 열어 **내 블로그ID** 넣기 (나머지는 ②분석이 채워줍니다)
4. **시작 파일 더블클릭**: 윈도우 `시작_윈도우.bat` / 맥 `시작_맥.command`
   → 처음엔 클로드 로그인(내 계정)

> ⚠️ 설치 파일이 **"스마트 앱 컨트롤이 차단했습니다"**라고 나오면:
> 받은 **압축파일**에 마우스 오른쪽 → 속성 → 맨 아래 **"차단 해제"** 체크 → 확인 → **다시 압축 풀기**.
> ("스마트 앱 컨트롤 끄기"는 하지 마세요. 한 번 끄면 다시 못 켭니다.)
> ⚠️ "파이썬이 없습니다"가 나오면 Microsoft Store에서 **Python 3.12** 설치 후 설치 파일 다시 더블클릭.

---

## ★ 프롬프트 카드 — 뭐라고 쳐야 하나 (이대로 순서대로)

### 폴더는 이 폴더를 지정하세요

`CLAUDE.md`와 `내정보.txt`가 **눈에 보이는 폴더**를 지정하세요.

```
📁 blog-auto-codex   ← 이 폴더를 지정
   ├ CLAUDE.md       ← 이게 보이면 맞습니다
   ├ 내정보.txt       ← 이것도 보이면 맞습니다
   └ run_ai_daily.py …
```

### 한 번에 하나씩, 순서대로 치세요

**① 시작**
```
CLAUDE.md 읽고 시작해줘
```

**② 내 블로그 분석** (내 블로그ID를 넣으세요. blog.naver.com/**abcd1234** 면 abcd1234)
```
내 블로그는 blog.naver.com/abcd1234 야. 분석해줘
```
→ 내 블로그 이름·주제·카테고리를 자동으로 읽어 **내정보.txt**를 채우고 **블로그분석.md**를 만들어 줍니다.

**③ 내 이야기 5개** (AI 글에 '진짜 내 이야기'를 넣기 위해)
```
내 이야기 5개 질문해줘
```
→ 다섯 가지를 물어봅니다. **짧게 답만** 하시면 **내이야기.md**에 정리됩니다.

**④ 네이버 로그인** (크롬 창이 뜨면 내가 직접 로그인)
```
네이버 로그인 시켜줘
```
→ 크롬 창이 뜨면 **5분 안에 내가 직접** 로그인하세요. (아이디·비번은 클로드가 대신 넣지 않습니다)

**⑤ 오늘 글 써서 임시저장까지**
```
오늘 글 써서 임시저장까지 해줘
```
→ 생성 → (코덱스 피드백 → 재작성) → 무료 사진 → **네이버 임시저장**까지 갑니다.
→ 끝나면 **네이버 글쓰기 → 저장 목록**에서 확인하고 **직접 발행**하세요. (발행 버튼은 내가 누릅니다)

> 처음엔 **"미리보기부터 보여줘"** 라고 해서 원고만 확인한 뒤 임시저장으로 넘어가면 안전합니다.

---

## 이미지 키 (선택 · 없어도 글은 나옵니다)

둘 다 **무료**입니다. 구독 안 해도 됩니다. 없으면 사진 자리가 `[사진N]`로 남고, 나중에 직접 넣으면 됩니다.

- **사진 키(Unsplash · 실사진 검색)** — https://unsplash.com/developers 에서 무료 발급 →
  내정보.txt 의 `사진 키:` 뒤에 붙여넣기. (기본으로 이걸 씁니다)
- **제미나이 키(AI가 그림 생성)** — https://aistudio.google.com/apikey 에서 무료 발급(하루 500장) →
  내정보.txt 의 `제미나이 키:` 뒤에 붙여넣기. (스톡에서 못 찾을 때 대신 그려줍니다)

---

## 코덱스(유료 챗지피티) 안내

- 이 패키지의 ② 피드백은 맥에 설치된 **ChatGPT 앱(유료 구독)**의 코덱스 기능을 씁니다.
- 없거나 로그인이 안 되면 **초안 그대로** 사진 넣어 임시저장까지 진행되고, 화면에 안내가 뜹니다.
- 피드백까지 자동으로 받고 싶은데 유료 구독이 없으면 **무료 패키지 ①(blog-auto-starter)**를 쓰세요.

---

## 안 될 때

- 화면에 빨간 글씨가 뜨면 **그 화면을 사진 찍어** 단톡방에 올려주세요.
- "네이버 로그인이 없습니다" → ④ 로그인을 다시 하고 ⑤를 한 번 더.
- 사진이 안 들어가고 `[사진N]`만 있으면 → 사진 키가 비었거나 검색 실패입니다. 직접 사진을 넣어 발행하세요.
